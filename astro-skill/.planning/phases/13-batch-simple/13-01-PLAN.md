---
phase: 13-batch-simple
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/astro-cloudflare/references/rendering-modes.md
autonomous: true

must_haves:
  truths:
    - "rendering-modes.md has 7 XML containers wrapping every ## section"
    - "Universal tags quick_reference, anti_patterns, troubleshooting are named identically to the pilot"
    - "All 6 grep patterns for rendering-modes.md in SKILL.md return exactly 1 match"
    - "git diff shows only added XML tag lines -- zero content modifications"
    - "Token overhead is below 5%"
  artifacts:
    - path: ".claude/skills/astro-cloudflare/references/rendering-modes.md"
      provides: "XML-tagged rendering modes reference"
      contains: "<quick_reference>"
  key_links:
    - from: ".claude/skills/astro-cloudflare/SKILL.md"
      to: "references/rendering-modes.md"
      via: "grep patterns"
      pattern: "grep -n .* references/rendering-modes.md"
---

<objective>
Apply XML semantic containers to rendering-modes.md -- the simplest reference file (7 sections, no subsections, no MCP callouts).

Purpose: First file in the batch validates the mechanical application of the pilot pattern.
Output: rendering-modes.md with 7 XML tags wrapping all ## sections, committed after validation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/XML-CONVENTIONS.md
@.claude/skills/astro-cloudflare/references/cloudflare-platform.md (pilot reference)
@.claude/skills/astro-cloudflare/references/rendering-modes.md
@.claude/skills/astro-cloudflare/SKILL.md (grep patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record baseline and apply 7 XML tags to rendering-modes.md</name>
  <files>.claude/skills/astro-cloudflare/references/rendering-modes.md</files>
  <action>
1. Record baseline: `wc -c .claude/skills/astro-cloudflare/references/rendering-modes.md`

2. Add XML container tags around each ## section. Tag map (7 tags):

| Line | Header | Tag |
|------|--------|-----|
| 3 | `## Quick Reference` | `<quick_reference>` |
| 14 | `## Output Modes` | `<output_modes>` |
| 54 | `## Decision Matrix` | `<decision_matrix>` |
| 69 | `## Server Islands` | `<server_islands>` |
| 122 | `## Feature Compatibility` | `<feature_compatibility>` |
| 135 | `## Anti-patterns` | `<anti_patterns>` |
| 149 | `## Troubleshooting` | `<troubleshooting>` |

Rules (from XML-CONVENTIONS.md):
- Opening tag goes immediately BEFORE the `##` header (no blank line between tag and header)
- Closing tag goes immediately AFTER the last content line of the section (no blank line before closing tag)
- Blank lines BETWEEN sections are preserved (between closing tag and next opening tag)
- File title `# Rendering Modes` gets NO tag wrapper
- DO NOT modify any content -- only add XML tag lines

3. Validate all 4 checks:

Check 1 -- Grep patterns (6 patterns, all must return exactly 1 match):
```bash
cd .claude/skills/astro-cloudflare
grep -n "## Output Modes" references/rendering-modes.md | wc -l          # expect 1
grep -n "## Decision Matrix" references/rendering-modes.md | wc -l       # expect 1
grep -n "## Server Islands" references/rendering-modes.md | wc -l        # expect 1
grep -n "## Feature Compatibility" references/rendering-modes.md | wc -l  # expect 1
grep -n "## Anti-patterns" references/rendering-modes.md | wc -l         # expect 1
grep -n "## Troubleshooting" references/rendering-modes.md | wc -l       # expect 1
```

Check 2 -- Diff integrity (only XML tag lines added):
```bash
git diff .claude/skills/astro-cloudflare/references/rendering-modes.md | grep '^+' | grep -v '^+++' | grep -v '^+$' | grep -v '^+</?[a-z_]*>$'
# Must return empty (no non-XML additions)
```

Check 3 -- Overhead:
```bash
wc -c .claude/skills/astro-cloudflare/references/rendering-modes.md
# Calculate: (after - before) / before * 100, must be < 5%
```

Check 4 -- Tag balance:
```bash
grep -c '^<[a-z_]*>$' .claude/skills/astro-cloudflare/references/rendering-modes.md   # expect 7
grep -c '^</[a-z_]*>$' .claude/skills/astro-cloudflare/references/rendering-modes.md  # expect 7
```

4. If all checks pass, commit:
```bash
git add .claude/skills/astro-cloudflare/references/rendering-modes.md
git commit -m "feat(13-01): apply XML semantic containers to rendering-modes.md"
```
  </action>
  <verify>All 4 validation checks pass (grep patterns, diff integrity, overhead < 5%, tag balance 7/7). Commit created.</verify>
  <done>rendering-modes.md has 7 XML containers, zero content changes, overhead < 5%, all grep patterns return 1 match, committed.</done>
</task>

</tasks>

<verification>
1. `grep -c '^<[a-z_]*>$' .claude/skills/astro-cloudflare/references/rendering-modes.md` returns 7
2. `grep -c '^</[a-z_]*>$' .claude/skills/astro-cloudflare/references/rendering-modes.md` returns 7
3. All 6 SKILL.md grep patterns for rendering-modes.md return exactly 1 match each
4. `git diff HEAD~1 -- .claude/skills/astro-cloudflare/references/rendering-modes.md | grep '^+' | grep -v '^+++' | grep -v '^+$' | grep -v '^+</?[a-z_]*>$'` returns empty
5. Overhead < 5%
</verification>

<success_criteria>
rendering-modes.md contains 7 XML semantic containers wrapping every ## section, with zero content modifications and all SKILL.md grep patterns returning exactly 1 match.
</success_criteria>

<output>
After completion, create `.planning/phases/13-batch-simple/13-01-SUMMARY.md`
</output>
